/*
 ╔═══════════════════════════════════════════════════════════════════════════╗
 ║                     (c) 1994,2002 корпорация ГАЛАКТИКА                    ║
 ║ Проект        : ГАЛАКТИКА                                                 ║
 ║ Система       : Налоговые регистры                                        ║
 ║ Версия        : 5.84+                                                     ║
 ║ Назначение    : Константы модуля "Налоговые регистры"                     ║
 ║ Ответственный : Станевич Юрий Олегович                                    ║
 ╚═══════════════════════════════════════════════════════════════════════════╝
*/

Const

// Режимы распределения принятой суммы

  nrRasprByMonth   = 0; // Пропорционально по месяцам
  nrRasprByYear    = 1; // Пропорционально по годам
  nrRasprByPerc    = 2; // В текущем периоде
  nrRasprByMnDt    = 3; // По месяцам с учетом даты
  nrRasprByKvart   = 4; // Пропорционально по кварталам
  nrRasprByKvDt    = 5; // По кварталам с учетом даты
  nrRasprByDay     = 6; // Пропорционально по дням
  nrRasprByDaySimp = 7; // По дням

// Значения по умолчанию для режима распределения

  nrRasprDefMode = nrRasprByMonth;
  nrRasprDefSrok = 12;

// Константы видимости документов для пользователя

  dcAllModif        = 0; // все права
  dcOwnOnly         = 1; // только со своим дескриптором
  dcGrpRead         = 2; // свои-изменение, по группе - чтение
  dcGrpModif        = 3; // все права в рамках группы
  dcOwnModifAllRead = 4; // свои - изменение, по всем - чтение
  dcGrpModifAllRead = 5; // все права в рамках группы, по всем - чтение
  dcReadOnly        = 6; // только чтение 

// end;
