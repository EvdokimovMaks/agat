//------------------------------------------------------------------------------
//                                            (c) 1994,2010 корпорация Галактика
// Галактика 8.10 - Контур логистики. Модуль "L_Smeta".
// Общие функции интерфейсов импорта смет и актов выполненных работ.
//------------------------------------------------------------------------------
#ifndef _ImportCommonFuncs_vih_
#define _ImportCommonFuncs_vih_

const
  // маски ошибок интерфейсов импорта актов, смет
  ERR_NO_IMPORT_SOURCE    = 0000000000000001b; // ошибка "не указан источник импорта"
  ERR_IMPORT_FILE         = 0000000000000010b; // ошибка "загрузка файла завершена с ошибками"
  ERR_IMPORT_CANCELED     = 0000000000000100b; // ошибка "импорт прерван пользователем"
  ERR_ANY_ERROR           = 1111111111111111b; // любая ошибка
end;

VipInterface ImportCommonFuncs
  licensed(free);
public:
  Procedure AddToProtocol( pwTable: word; pcRec: comp );
  Function GetKatStroy( psName: string[200]; isForceMakeObj: boolean ): comp;

  function IsErrorFlagSet( _errVar: word; _errMask: word ): boolean;
  procedure SetErrorFlag( var _errVar: word; _errMask: word );
  procedure ClearErrorFlag(  var _errVar: word; _errMask: word );
end;
#endif
