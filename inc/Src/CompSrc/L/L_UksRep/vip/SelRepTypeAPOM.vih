//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.10 - Логистика.
// Выбор типа отчета для актов передачи оборудования в монтаж.
//------------------------------------------------------------------------------
#ifndef _SelRepTypeAPOM_vih_
#define _SelRepTypeAPOM_vih_

#component "L_UKSREP"

const
  APOM_REP_WAYBILLS = 0; // накладные
  APOM_REP_OS15 = 1; // форма ОС-15
  APOM_REP_UNKNOWN = rvMaxWord;
end;

ObjInterface objSelRepTypeAPOM;

#doc
Вывести диалог выбора типа отчета и вернуть выбор пользователя.
#end
function SelectReport(
  out _reportType: word; // #docl Возвращает тип выбранного отчета (возможные типы отчетов - см. константы APOM_REP_* в VIH)
  out _showTotals: boolean // #docl Актуально для ОС-15. Выводить ли строку "Итого" в отчете.
  ): boolean; // #docl true - пользователь сделал выбор, false - в противном случае.

end;

public VipInterface SelRepTypeAPOM implements objSelRepTypeAPOM Licensed (FREE);

#endif
