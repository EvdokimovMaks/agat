.begin
  sForestry   := ''; // лесничество
  sRound      := ''; // обход
  sQuarter    := ''; // квартал
  sAllocation := ''; // выдел
  sCutting    := ''; // лесосека
  sLesnik     := ''; // лесник

  if (nnn.GetFirst KatSopr = tsOk)
    if (nnn.KatSopr.cOtvPodr <> comp(0))
      RunInterface('GetWFondName', nnn.KatSopr.cOtvPodr, sForestry, sRound, sQuarter, sAllocation, sCutting, sLesnik);
end.