//******************************************************************************
//                                                      (c) корпорация Галактика
// Галактика 8.1 - Логистика
// Константы, описывающие режимы функций работы с ордерами (параметр wMode)
//******************************************************************************
#ifndef _OrderFuncModes_inc_
#define _OrderFuncModes_inc_

const
  // Битовые маски параметра wMode - режим функций работы с ордерами
  WMODE_BATCH           = 1; // бит, указывающий на пакетный (запросы на ввод данных пользователю не должны бы выдаваться) режим работы.
  WMODE_REQUEST_DPRICE  = 2; // бит, указывающий на необходимость выдавать запрос на ввод даты цен при выполнении условий: if(boGetTune('Oper.Buy.Nakl.dPrice')) And (KatSopr.cVal <> 0) And (KatSopr.vidSopr = 101 Or KatSopr.vidSopr = 108)
  WMODE_LOG_TO_MSGBATCH = 4; // при установленном бите сообщения протоколируются в объект MsgBatch
  // значение 128 (последний бит!!!) зарезервировано внутри makeorder_rash.vpp
end;

#endif
