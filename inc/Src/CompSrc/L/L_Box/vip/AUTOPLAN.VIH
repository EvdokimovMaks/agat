#ifndef _AUTOPLAN_VIH
#define _AUTOPLAN_VIH

#ifdef ComponentVersion
#component "L_Box"
#end

#doc
Объект управления планом
#end
ObjInterface tBoxAutoPlanObjInterface;
#doc
Функция проверки использования ячеек
#end
  function UseBox(mess : boolean): boolean;

#doc
Проверка на использование плана для ячеек
#end
  Function IsPlanExist (_cSpSopr : comp; _cPodr : comp) : boolean;

#doc
Печать плана
#end
  Function PrintAllPlans (_cKatSopr : comp) : boolean;

#doc
Удаление всех планов для ячеек
#end
  Function DeleteAllPlans (_cKatSopr : comp) : boolean;

#doc
Функция удаления ранее сформированного плана
#end
  Function DeletePlan (_cSpSopr : comp; _cPodr : comp) : boolean;

#doc
Удаление плана размещения МЦ по ячейкам хранения (по партиям)
#end
  Function DeletePlanByParty (_cSpSopr : comp; _cParty : comp) : boolean;

#doc
Удаление плана размещения МЦ по ячейкам хранения (по МЦ)
#end
  Function DeletePlanByMC (_cSpSopr : comp; _cMC : comp) : boolean;

#doc
Функция автоформирования нового плана (старый автоматич. удаляется)
#end
  Function MakePlan (_cSpSopr : comp; _cSoprOrdB : comp) : boolean;

#doc
Функция автоформирования планов по всем SpSopr (старые автоматич. удаляется)
#end
  Function MakeAllPlans (_cKatSopr : comp) : boolean;

#doc
Функция автоформирования планов по SpOrder - для интерфейсов ордеров и пр.
#end
  Function MakePlanSpOrder (_cSpOrder: comp; show_mess: boolean): boolean;

#doc
Функция автоформирования нового плана по позиции НВП (старый автоматич. удаляется)
#end
  Function MakePlanMove(_cKatSopr, _cSpSopr : comp; show_mess: boolean): boolean;

#doc
Функция автоформирования планов по всем SpSopr для НВП (старые автоматич. удаляется)
#end
  Function MakeAllPlansMove (_cKatSopr: comp): boolean;
end;

ObjInterface tBoxAutoPlanObjInterfaceV2(tBoxAutoPlanObjInterface);

#doc
Функция автоформирования плана по SpSopr для пересортицы (старый удаляется)
#end
Function MakePlanPeres (_cKatSopr, _cSpSopr: comp): boolean;

#doc
Печать распределения по ячейкам хранения в ордере
#end
Function PrintAllRaspOrd (_cSklOrder: comp): boolean;

end;

VipInterface BoxAutoPlan implements tBoxAutoPlanObjInterfaceV2 licensed(FREE);
#end
