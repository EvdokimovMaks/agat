.var
  StrNN,
  Nom,
  NomFull,
  kolAdd,            // кол-во недопечатанных строк на текущей странице
  MainKol,           // фиксированное кол-во строк на первой странице
  KolPage,           // счетчик обработанных строк на текущей странице
  FixKol,            // фиксированное кол-во строк на страницах, начиная со второй
  KolSpSopr,
  FixKolPage,        // допустимое кол-во строк на текущей странице
  breakPage,
  wForm
             : word;

  Sum1ItogFull,
  Sum2ItogFull,
  Sum3ItogFull,
  Sum4ItogFull,
  Sum5ItogFull,
  Sum6ItogFull,
  Sum7ItogFull,
  Sum8ItogFull
             : double;
  _su1, _su2, _su3 : double; // Для подсчета итогов

  NextPage,
  bAutoGrM,          // автоматический расчет кол-ва грузовых мест
  bRunSign
             : boolean;

  СпСимвол,
  strEmpty,
  Cena_Pr0,
  KolGrM,
  KolOneGrM,
  sFIO1, sPost1,
  sFIO2, sPost2,
  sFIO3, sPost3,
  sFIO4, sPost4,
  sFIO5, sPost5,
  sFIO6, sPost6,
  sFIO7, sPost7,
  sFIO8, sPost8
             : string;

.endvar
.begin

  // инициализация переменных
  oPrnSoprFun.Init(0, 0, wRoundMass2, false, false,
                   false, Massa_Tn, Massa_Kg, Massa_Gr, Cena_Pr, Cena_PrR, NDS_St, RoundGrm);

#ifdef _UniversalKor_
  bRunSign := not boGetTune('Doc.RunSign');
  wForm    := 12;
#else
  bRunSign := not boGetTune('Doc.RunSign');
  wForm    := 7;
#end

#ifdef _TTN_SKL_ORG_
  #undef _TTN_SKL_ORG_
#end
end.
