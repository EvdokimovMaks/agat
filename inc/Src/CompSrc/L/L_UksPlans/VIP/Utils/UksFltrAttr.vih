//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 5.8х - УКС - Управление капитальным строительством
// Фильтр по атрибутам объектов строительства
//------------------------------------------------------------------------------

#ifdef ComponentVersion
 #component "L_UksPlans"
#end

#doc
Содержит методы для работы с фильтром по атрибутам объектов строительства.
#end
ObjInterface OUksFltrAttr;

!--------------------------------------------------------------------------
#doc
Запуск диалога фильтра. Возвращает TRUE, если фильтр установлен.
#end
  Function Run(var asFilter : string) : boolean;
!--------------------------------------------------------------------------
#doc
Очистка фильтра.
#end
  Procedure Clear;
!--------------------------------------------------------------------------
#doc
Отбор объектов строительства, удовлетворяющих заданным
ограничениям и сохранение их в маркере MarkerKatStroy.
Возвращает через alQnt количество объектов, сохраненных в маркере.
#end
  Function Convert(var alQnt : longint; var acPtr : comp) : boolean;
!--------------------------------------------------------------------------
#doc
Проверка на удовлетворение фильтру.
#end
  Function Satisfy(aiMode : integer; acRec : comp) : boolean;

End;

VipInterface IUksFltrAttr implements OUksFltrAttr licensed(FREE);
