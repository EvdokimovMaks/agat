//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 9.1 - Логистика
// Настройка соответствия полей ЭСЧФ
//------------------------------------------------------------------------------
#ifndef __TuneUDR_vih__
#define __TuneUDR_vih__

#Component "L_SF"

#include TuneUDR.inc

#doc
Настройка соответствия полей ЭСЧФ
#end
ObjInterface ObjTuneUDR;

  #doc
  Тип поля ЭСЧФ
  #end
  Function GetType(fiField: word): word;

  #doc
  Индекс элемента ResWord в соответствии с полем ЭСЧФ
  #end
  Function numResWord(fiField: word): word;

  #doc
  Индекс элемента ResName в соответствии с полем ЭСЧФ
  #end
  Function numResName(fiField: word): word;

  #doc
  Индекс элемента ResDate в соответствии с полем ЭСЧФ
  #end
  Function numResDate(fiField: word): word;

  #doc
  Индекс элемента в соответствии с полем ЭСЧФ
  #end
  Function numRes(fiField: word): word;

  #doc
  Наименование поля таблицы
  #end
  Function GetNameList(fdField: word; bShort: boolean = false): string;

  #doc
  Код таблицы поля
  #end
  Function GetCodeTableFromField(fdField: word): word;

  #doc
  Тип поля таблицы
  #end
  Function GetTypeField(fdField: word): word;

  #doc
  Код вида документа, подтверждающего поставку
  #end
  Function TypeDocToWord(sTypeDoc: string): word;
End;

VipInterface TuneUDR
  implements ObjTuneUDR
  licensed (FREE);

#end // __TuneUDR_vih__
