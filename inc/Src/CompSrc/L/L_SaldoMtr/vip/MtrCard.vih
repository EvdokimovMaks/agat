//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 7.12 - Целевой учет запасов
// Карточка учета МТР по объекту целевого назначения
//------------------------------------------------------------------------------

#ifndef __MtrCard_vih__
#define __MtrCard_vih__

#component "L_SaldoMtr"

#include MtrCard.tbl

#doc
Содержит методы для работы с карточкой запасов МТР.
#end
ObjInterface OMtrCard;
!--------------------------------------------------------------------------
#doc
Инициализация настройки объектов целевого учета.
#end
  Function InitType(awType, awMode : word) : boolean;
!--------------------------------------------------------------------------
#doc
Установка ссылки на объект целевого учета.
#end
  Procedure SetParam(apiMode : integer; acObj : comp);
!--------------------------------------------------------------------------
#doc
Установка показа своих(чужих) МТР.
#end
  Procedure SetMtrParam(apiMtrMode : word);
!--------------------------------------------------------------------------

#doc
Получение общей стоимости запасов МТР по текущему объекту целевого учета.
#end
  Procedure GetSumm(var afTotalRub, afTotalVal : double);
!--------------------------------------------------------------------------
#doc
Изменение параметров для заголовка интерфейса.
#end
  Event Procedure doSetTitle(abFilter : boolean; asFltrName : string; alSelQnt : longint);
!--------------------------------------------------------------------------
#doc
Нажат Enter.
#end
  Event Procedure doEnter;

#doc
Снятие фильтра.
#end
  Procedure ResetSaldoFilter(abSaldo, abKau, abPeriod : boolean);
End;

VipInterface MtrCard implements OMtrCard
  Licensed(FREE)
  Parameters(pwType: word; pcObj: comp);

#end // __MtrCard_vih__