//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика ERP 9.1 - Заработная плата
// Интерфейс-стрелочник для вызова отчетов компонента Z_Report
//------------------------------------------------------------------------------

#component "Z_PFRep"

ObjInterface IRepRunner;
  procedure Run(_RepNum: word);
end;

VipInterface RepRunner
  implements IRepRunner
  parameters(RepNum: word)
  licensed(free);
