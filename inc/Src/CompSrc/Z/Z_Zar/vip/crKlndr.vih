//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 5.0 - Заработная плата
// Формирование табеля (нигде не вызывается)
//------------------------------------------------------------------------------

#component "Z_Zar"

ObjInterface oCreateTimeTable;
  function CreateItemInZarklndr(RecNumber: comp; BeginningDate: date; EndingDate: date; OperatingMode: word): boolean;
End;
