//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.1 - Общая функциональность работы с допаналитиками
// Функции работы с КАУ
//------------------------------------------------------------------------------

#ifndef _ExtrKau_vih_Included
#define _ExtrKau_vih_Included

#component "Z_Zar"

#doc
Объектный интерфейс "Функции работы с допаналитиками"
#end
ObjInterface IExtrKauFunctions;

#ifdef Gal9_1
  #doc
  функция нахождения ссылки на подразделение по доп. аналитике корневых таблиц
  #end
  function fcGetDopAnPodrExtr( KauExtr: tGetKau; TblExtr: tTabKau ) : comp;

  #doc
  функция определения списка КАУ корневых таблиц
  #end
  function fcGetNameExtrKau( KauExtr: tGetKau; TblExtr: tTabKau ) : string;

  #doc
  функция нахождения подразделения для отнесения затрат на основе доп.аналитик корневых таблиц
  #end
  function fcGetCexoz_From_Extr(cnRec_Cexoz: comp; KauExtr: tGetKau; TblExtr: tTabKau; var cnRec_DopAn: comp): comp;

  #doc
  функция определения наименования КАУ в корневых таблицах на основе доп.аналитик корневых таблиц
  #end
  function fShowKauExtr(KauExtr: tGetKau; TblExtr: tTabKau; wTable : word ) : string;
#end

  #doc
  функция нахождения ссылки на подразделение по доп. аналитике в таблице OtpDop
  #end
  function fcGetDopAnPodr( cnRec_OtpDop : comp ) : comp;

  #doc
  функция определения наименования КАУ в таблице OtpDop
  #end
  function fcGetNameKau( cnRec_OtpDop : comp ) : string;

  #doc
  функция нахождения подразделения для отнесения затрат на основе доп.аналитик в таблице OtpDop
  #end
  function fcGetCexoz(cnRec_Cexoz, cnRec_OtpDop: comp; var cnRec_DopAn: comp): comp;
  #doc
  функция определения наименования КАУ в корневых таблицах на основе доп.аналитик в таблице OtpDop
  #end
  function fShowKau( cOtpDop : comp; wTable : word ) : string;

end;

ObjInterface IExtrKauFunctions_1;
#ifdef Gal9_1
  #doc
  функция нахождения ссылки на доп. аналитику по TblCode корневых таблиц
  #end
  function fcGetDopAnExtr( TblCode : word;  KauExtr: tGetKau; TblExtr: tTabKau ) : comp;

#end

  #doc
  функция нахождения ссылки доп. аналитике TblCode в таблице OtpDop
  #end
  function fcGetDopAn( TblCode : word; cnRec_OtpDop : comp ) : comp;

end;

VipInterface ExtrKauFunctions implements IExtrKauFunctions, IExtrKauFunctions_1
licensed (free);
#end
