//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика ERP 9.1 - Заработная плата
// Настройка расчета удержаний межпериода
//------------------------------------------------------------------------------

#component "Z_Sredn"

#include CalcUderSet.inc // Константы для параметров расчета удержаний межпериода

VipInterface CalcUderSet
  licensed(free)
  parameters(prmFlags, prmFrom: word);
