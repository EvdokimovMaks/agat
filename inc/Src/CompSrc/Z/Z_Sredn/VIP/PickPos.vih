//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 9.1 - Заработная плата
// Виды пособий
//------------------------------------------------------------------------------

#component "Z_Sredn"

ObjInterface objPickPos;
event procedure pGetVidPos(VidPos_: word);
procedure pSetParam(VidPos_: word; sAttrVal_: string);
end;

vipinterface PickPos implements objPickPos licensed(free)
parameters(VidPos_t: word);
