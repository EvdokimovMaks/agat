// Галактика 9.1
// Описание экспортируемых интерфейсов компонента

#component "Z_FSZN"

vipinterface PU3_2014 licensed(free) parameters (PUN: objRef);
vipinterface PU3_2016 licensed(free) parameters (PUN: objRef);
vipinterface PU6 licensed(free);
vipinterface PU_6 licensed(free) parameters (PUN: objRef);
