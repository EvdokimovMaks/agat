//******************************************************************************
//                                                      (с) корпорация Галактика
// Галактика 7.12
// Объектный интерфейс для корректировки реестров из платежных поручений
//******************************************************************************
#ifndef _EditReesFuncs_vih_INCLUDED_
#define _EditReesFuncs_vih_INCLUDED_

#include IProtectable.obj

#component "Z_PayRep"

#doc
Объектный интерфейс для корректировки реестров из платежных поручений
#end
objInterface IEditReesFuncs;
  #doc
  редактирование реестра
  #end
  function EditReestr(SbBanRef: comp): double;
  #doc
  создание реестра для платежного поручения
  #end
  function CreateReestr(PlPorRef: comp): double;
end;

objinterface IViewSpecification;
  #doc
    Настраивает браузер на просмотр спецификации данного владельца.
  #end
  procedure LinkTo(AOwner: word; ARec: comp);
  #doc
    Устанавливает режим редактирования (true) или просмотра (false)
  #end
  procedure SetEditMode(Value: boolean);
  #doc
    Перерисовывает содержимое браузера.
  #end
  procedure ReDraw;
  #doc
    Обновляет и перерисовывает содержимое браузера.
  #end
  procedure ReRead;
  #doc
    Возникает после обновления, удаления, обновления записи/группы записей.
  #end
  event procedure OnModeChanged(Value: boolean);
  #doc
    Возникает после обновления, удаления, обновления записи/группы записей.
  #end
  event procedure OnListChanged;
end;

vipInterface BrowRees
  implements
    IEditReesFuncs,
    IViewSpecification,
    IProtectable
  licensed (free)
parameters(WorkNrec: comp; Options: longint; cSpec: comp)
;

vipInterface BrowPved
  implements
    IViewSpecification,
    IProtectable
  licensed (free)
parameters(WorkNrec: comp; Options: longint; cSpec: comp)
;

#end
