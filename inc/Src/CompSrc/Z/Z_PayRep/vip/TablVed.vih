//-----------------------------------------------------------------------------
//                                                     (c) корпорация ГАЛАКТИКА
// Галактика 8.1 - Заработная плата
// Список ведомостей (и выбор на основе параметров, переданных через буфер)
//-----------------------------------------------------------------------------
#ifndef _TablVed_vih_INCLUDED_
#define _TablVed_vih_INCLUDED_

#component "Z_PayRep"

objinterface IPickPved;
  #doc
    Выбор платежных ведомостей на основе параметров, переданных через буфер
  #end
  function Pick(B: type$PlatVed): boolean;

end;
//------------------------------------------------------------------------------
vipInterface TablVed
  implements
    IPickPved
  licensed(free)
parameters(TM: word);

#end
