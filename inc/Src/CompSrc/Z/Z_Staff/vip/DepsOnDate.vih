//------------------------------------------------------------------------------
//                                                      (с) корпорация Галактика
//  Галактика ERP 8.1 - Управление персоналом
//  Объектный интерфейс "Подразделения предприятия на дату"
//------------------------------------------------------------------------------

#ifndef _DepsOnDate_vih_Included
#define _DepsOnDate_vih_Included

#ifdef ComponentVersion
  #component "Z_Staff"
#end
#doc
  Объектный интерфейс, позволяющий взаимодействовать с временной таблицей с подразделениями предприятия на дату
#end
objinterface IDepsOnDate;

#doc
  Заполнить временную таблицу данными о подразделениях на дату
#end
  procedure InitTmp(dInit: date);

#doc
  Показать подразделения из временной таблицы.
  Возвращает истину, если окно закрылось по cmDefault.
#end
  function ShowDeps: boolean;
end;

#doc
  Расширение объектного интерфейса, позволяющего взаимодействовать с временной таблицей с подразделениями предприятия на дату
#end
objinterface IDepsOnDate_2;

#doc
  Добавить префикс к используемому маркеру
    _sPrefix - префикс маркера
#end
  procedure AddMarkerPrefix(_sPrefix: string);

#doc
  Заполнить временную таблицу данными о подразделениях на период
#end
  procedure InitTmp4Period(_dBeg, _dEnd: date);
end;

vipinterface DepsOnDate implements IDepsOnDate, IDepsOnDate_2
  Licensed(Free)
;

#endif
