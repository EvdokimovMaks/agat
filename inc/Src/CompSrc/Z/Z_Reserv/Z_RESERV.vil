// Галактика 9.1
// Описание экспортируемых интерфейсов компонента

#component "Z_Reserv"

VipInterface RESERV_Param licensed(free);
VipInterface RESERV_print licensed(free);


VipInterface TransferToFundsAccounts
  licensed(free)
  parameters (Markers: TPtr; ASource: word);

VipInterface RESERV_Param_inv licensed(free) parameters(_isFromZarpl: boolean);
