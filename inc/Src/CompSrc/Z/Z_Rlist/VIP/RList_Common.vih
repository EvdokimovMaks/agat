//-----------------------------------------------------------------------------
//                                                     (c) корпорация ГАЛАКТИКА
// Галактика 8.10 - заработная плата
// Временные таблицы для формирования РСВ-1
//-----------------------------------------------------------------------------

#ifndef _RLIST_COMMON_VIH_INCLUDED_
#define _RLIST_COMMON_VIH_INCLUDED_

#component "Z_RList"

const
  cSocnalPaymentSolidPart = 'Л'; // солидарная часть взносов
end;

//------------------------------------------------------------------------------
// Таблица для агрегирования сумм по ЕСН за год
//------------------------------------------------------------------------------
table struct tmpESN
(
  NALCODE  : WORD       "Код налога" ,
  NALNAME  : string[50] "Название налога" ,
  SummaVIP : double     "Сумма с которой брали",
  SumBase  : double     "База для начисления, не прев. предельную",
  NALSumma : double     "Сумма налога"
)
with index
(
  ESN00   = NALCODE
);

//------------------------------------------------------------------------------
// Табличка, для агрегирования сумм по налогам на ФОТ, за месяц
//------------------------------------------------------------------------------
table struct tmpNFOT
(
  NALCODE: WORD       "Код перечисления" ,
  NALNAME: string[50] "Название налога" ,
  NALSumma: double     "Сумма налога"
)
with index
(
  NFOT00   = NALCODE
);

#end
