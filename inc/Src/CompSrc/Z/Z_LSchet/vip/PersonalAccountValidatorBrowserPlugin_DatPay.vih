//------------------------------------------------------------------------------
//                                                      (с) корпорация Галактика
// Галактика 9.1 - Заработная плата
// Валидатор лицевого счета по дате выплаты
//------------------------------------------------------------------------------

#include Marks.vih

#component "z_lschet"

// Валидатор лицевого счета по дате выплаты
// (валидным считается лицевой счет c датой выплаты, попадающей в заданный период)

vipinterface PersonalAccountValidatorBrowserPlugin_DatPay
  implements IPersonalAccountValidatorBrowserPlugin, IResourceBased
  licensed(free);
