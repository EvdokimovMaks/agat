//******************************************************************************
//                                                      (с) корпорация Галактика
// Галактика 8.10
// перемещение работника в другие филиал и юридическое лицо
//******************************************************************************

#ifndef _MoveLschFilialArtPers_vih_INCUDED_
#define _MoveLschFilialArtPers_vih_INCUDED_

#ifdef ComponentVersion
#component "Z_Lschet"
#end

ObjInterface IMoveLschFilialArtPers;
#doc
  Функция создания лицевого счета в новом филиале
#end
function CreateNewLschForFilial(
                             cOldLSchet:comp;
                             LsPerson:comp;
                             NewCex:  comp;
                             NewTabN:longint;
                             NewStrTabn: string;
                             nRecFilial:comp;// новый филиал
                             var NewnRec:comp):boolean;
#doc
  функция переноса данных при переводе сотрудника между разными юридическими лицами
#end
procedure MoveTablesDiffPredp(_cLschet:comp; newLsch:comp; cPerson: comp;NewAtl_Branch:comp);

#doc
  функция переноса данных при переводе сотрудника из филиала в филиал в рамках одного юр лица
#end
// Устаревшая процедура не пользоваться!!!! более новый аналог MoveTablesOnePredp
procedure MoveTablesInOnePredp(_cLschet:comp; newLsch:comp; cPerson: comp; NewAtl_Branch:comp);
end;

#doc
Расширение интерфейса IMoveLschFilialArtPers
#end
ObjInterface IMoveLschFilialArtPers1;

#doc
  функция переноса данных при переводе сотрудника из филиала в филиал в рамках одного юр лица
#end
procedure MoveTablesOnePredp(_cLschet: comp;
                              newLsch: comp;
                              cPerson: comp;
                        NewAtl_Branch: comp;
                                param: word);
end;

#doc
Расширение интерфейса IMoveLschFilialArtPers
#end
ObjInterface IMoveLschFilialArtPers2;

#doc
  функция переноса данных при повторном приёме сотрудника в новый филиал в рамках разных юр.лиц
#end
procedure MoveDataDiffPredp(_cLschet: comp;
                             newLsch: comp;
                             cPerson: comp;
                       NewAtl_Branch: comp;
                               param: word);
end;

#doc
Расширение интерфейса IMoveLschFilialArtPers
#end
ObjInterface IMoveLschFilialArtPers3;

procedure MoveOrSyncTablesOnePredp( _cLschet: comp;
                                    newLsch: comp;
                                     OldCex: comp;
                                     Period: date;
                                  dataGroup: word;
                                    cPerson: comp;
                              NewAtl_Branch: comp;
                                      param: word;
                                wMoveOrSync: word);
end;

ObjInterface IMoveLschFilialArtPers4;
#doc
  новая функция переноса данных при переводе/синхронизации сотрудника между разными юридическими лицами
#end
procedure MoveOrSyncTablesDiffPredp( _cLschet: comp;
                                     newLsch: comp;
                                      OldCex: comp;
                                      Period: date;
                                   dataGroup: word;
                                     cPerson: comp;
                               NewAtl_Branch: comp;
                                 wMoveOrSync: word);
end;

vipinterface MoveLschFilialArtPers 
             implements IMoveLschFilialArtPers, IMoveLschFilialArtPers1, IMoveLschFilialArtPers2, IMoveLschFilialArtPers3, IMoveLschFilialArtPers4 licensed(free);

#end
