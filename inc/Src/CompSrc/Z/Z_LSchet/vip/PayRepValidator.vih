//------------------------------------------------------------------------------
//                                                      (с) корпорация Галактика
// Галактика 9.1 - Заработная плата
// Валидатор по наличию лицевого счета в платежных ведомостях
//------------------------------------------------------------------------------
#ifndef _PayRepValidator_vih_INCLUDED_
#define _PayRepValidator_vih_INCLUDED_

#include Marks.vih
#include DefaultPaValidators.obj

#component "Z_LSchet"

//******************************************************************************
#doc
  Объектный интерфейс расширенной настройки параметров валидатора наличия
  лицевого счета в платежных ведомостях
#end
objinterface IPlatVedValidatorOptions;
  #doc
    Флаги признаков обработки ведомости
  #end
  procedure SetObrab(Obrab: longint);

  #doc
    Флаги проверки наличия, либо отсутствия выплат в ведомости
  #end
  procedure SetExist(Exist: longint);

  #doc
    Флаги опций
  #end
  procedure SetFlags(Flags: longint);

  #doc
    Очищает текущий список префиксов
  #end
  procedure Clear;

  #doc
    Добавляет префикс в текущий список
  #end
  procedure Add(Pref: s5);

end;

//******************************************************************************
#doc
  Валидатор лицевого счета по "наличию в платежных ведомостях"
#end
vipinterface DefaultPayRepValidator
  implements
    IPersonalAccountValidator,
    IAccountingMonthValidator,
    IPlatVedValidatorOptions
  Licensed(Free);

//******************************************************************************
#doc
  Плагин, представляющий валидатор лицевого счета по "наличию в платежных ведомостях"
  в браузере валидаторов лицевого счета
#end
vipinterface PersonalAccountValidatorBrowserPlugin_PayRep
  implements IPersonalAccountValidatorBrowserPlugin, IResourceBased
  Licensed(Free);


#end
