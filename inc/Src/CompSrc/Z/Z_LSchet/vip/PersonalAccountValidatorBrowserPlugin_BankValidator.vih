//------------------------------------------------------------------------------
//                                                      (с) корпорация Галактика
// Галактика 9.1 - Заработная плата
// Валидатор по банку в лицевом счете
//------------------------------------------------------------------------------

#include Marks.vih

#component "Z_LSchet"

#doc
  Плагин, представляющий валидатор лицевого счета по банку в браузере
  валидаторов лицевого счета
#end
vipinterface PersonalAccountValidatorBrowserPlugin_BankValidator
  implements IPersonalAccountValidatorBrowserPlugin, IResourceBased
  Licensed(Free);
