//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика ERP 9.1 - Заработная плата
// Общие параметры для интерфейсов переходов
//------------------------------------------------------------------------------

#ifNdef _PEREXODINTERFACEPARAM_VIH_INCLUDED_
#define _PEREXODINTERFACEPARAM_VIH_INCLUDED_

#doc
  Общие параметры интерфейсов переходов
#end
objInterface IPerexodInterfaceParam;
  #doc
    Прочесть параметры из DSK
  #end
  procedure ReadFromDSK;
  #doc
    Сохранить параметры в DSK
  #end
  procedure WriteToDSK;

  #doc
    <brief>Режим отображения доплат в переходах</brief>
    <p>0 - пересекаемые переходом, 1 - привязанные к переходу</p>
  #end
  property DoplataShowingMode: word read write;
end;

// Канонiчная реализация.
// При загрузке автоматически делает ReadFromDSK
vipInterface PerexodInterfaceParam implements IPerexodInterfaceParam licensed(free);

#endif
