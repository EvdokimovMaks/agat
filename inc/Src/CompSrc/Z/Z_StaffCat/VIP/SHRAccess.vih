//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.1 - Управление персоналом
// Разграничения доступа к СЕ ШР
//------------------------------------------------------------------------------

#ifndef _SHRAccess_INCLUDED_
#define _SHRAccess_INCLUDED_

#component "Z_StaffCat"

#doc
Проверка доступа к СЕ ШР
#end
ObjInterface objSHRAccess;

deprecated 'Функция, описанная в objSHRAccess, работают с стоковым идентификатором пользователя. Они работают не точно, т.к. при NT-авторизации есть вероятность возникновения двух одинаковых строковых идентификаторов. Используйте метод, описанные в objSHRAccess2.';

#doc
Проверка доступа к ставке по ее категории
#end
function GetKategoryAccess(cKat: comp;   // #docl Ссылка на категорию
                           sUser: string // #docl Имя пользователя
                          ): boolean;

end;

ObjInterface objSHRAccess2;

#doc
Проверка доступа к ставке по ее категории и UserID
#end
function GetKategoryAccessByID(cKat: comp; cUser: comp): boolean;

end;

VipInterface SHRAccess implements objSHRAccess, objSHRAccess2
  Licensed (free);

#end
