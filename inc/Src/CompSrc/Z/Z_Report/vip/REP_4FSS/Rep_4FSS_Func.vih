//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика ERP 9.1 - Зарплата
// Назначение   : Общие функции для отчета 4-ФСС (2017)
//------------------------------------------------------------------------------

#ifndef _Rep_4FSS_Func_VIH_INCLUDED_
#define _Rep_4FSS_Func_VIH_INCLUDED_

#component "Z_Report"

#doc
Общие функции для отчета 4-ФСС (2017)
#end

table struct tmp_ClassProfRisk
(
  wClass  : word,           // класс профессионального риска
  doProc  : TSumma,
  sProc   : string
)
with index
(
  tmp_ClassProfRisk01 = sProc
);

table struct XMLPayOrder
(
  NumbRazdel : word,
  Num        : string[10],
  DateOrder  : date,
  Summa      : double
)
with index
(
  XMLPayOrder01 = NumbRazdel
);

ObjInterface iRep_4FSS_Func;
function GetBit(Bit: word; Value: word): boolean;
procedure SetBit(Bit: word; var Value: word);
#doc
 Функция проверяет наличие данных в Разделах формы 4-ФСС
#end
function fboCheck_Table_4FSS(cRecDocum_, cRec_Table_: comp; wNumTable: word): boolean;
function fwGetFontColor(wPriznEdit: word): word;
#doc
 Функция возвращает значение последнего месяца периода
#end
function GetMonthEnd(wRepPeriod: word): word;
end;

ObjInterface iRep_4FSS_Func2;
#doc
 Процедура создания классификатора классов профессионального риска и соответствующих им процентов
#end
procedure pFillClassProfRisk;

#doc
 Процедура определения класса профессионального риска по проценту
#end
function fSetClassProfRisk(_wClass: TSumma): word;

#doc
 Процедура восстановления временной таблицы с платежными поручениями из деска
#end
procedure InitXMLPayOrder;
end;

VipInterface Rep_4FSS_FUNC implements iRep_4FSS_FUNC, iRep_4FSS_Func2
  licensed(free);
#end
