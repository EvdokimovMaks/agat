
//******************************************************************************
//                                                      (с) корпорация Галактика
//  Галактика 8.1
//  Функции получения штрих кода в EAN13
//******************************************************************************

#ifndef __EanBarCode__vih__
#define __EanBarCode__vih__

#component "C_Common"

#doc
Функции перевода числового штрих кода EAN13 в символы шрифта EanGnivc.ttf
Данный шрифт применяется для печати штрих кодов в RTF и SLK отчетах
#end
VipInterface EanBarCode licensed (Free);
  static:
  #doc
    Получить последовательность символов для печати спомошью шрифта EanGnivc.ttf 
    На основании 13 символьной строки штрих кода EAN13
    Например из строки 122234343443 должна быть сформирована цепочка $!22C3ED-edeedh!
  #end
  Function Ean13ToEanGnivcTTF(sInput: String): String;


  #doc
    Получить последовательность символов для печати спомошью шрифта EanGnivc.ttf 
    На основании 12 символьной строки штрих кода EAN13, 13-й символ контролной суммы формируется автоматически
    Например из строки 122234343443 должна быть сформирована цепочка $!22C3ED-edeedh!
  #end
  Function Ean13NoCheckSumToEanGnivcTTF(sInput: String): String;

  #doc
    Генерация цифры "проверочной суммы"
  #end
  Function GetCheckDigitForBarCode (subBarCode: String): Word;
End;

#endif