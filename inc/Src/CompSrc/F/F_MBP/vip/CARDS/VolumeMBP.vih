//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 9.1 - Спецоснастка
// Данные об объемах производства для спецоснастки
//------------------------------------------------------------------------------

#ifndef __VOLUME_MBP_VIH__
#define __VOLUME_MBP_VIH__

#ifdef ComponentVersion
#Component "F_MBP"
#end

#doc
Данные об объемах производства для спецоснастки.<br>
#end
ObjInterface objVolumeMBP;

  #doc
  Отображение данных об объемах производства.<br>
  #end
  Procedure ShowVolume(pMode : word; pIn : comp);

  #doc
  Удаление данных об объемах производства.<br>
  #end
  Function DeleteVolume(pIn : comp) : boolean;

  #doc
  Копирование данных об объемах производства из родительского прихода в дочерний.<br>
  #end
  Function CopyVolume(pInFr, pInTo : comp) : boolean;

  #doc
  Получение данных о ресурсе объекта.<br>
  #end
  Function GetMaxVolume(pIn : comp) : double;

  #doc
  Получение данных об объемах производства за указанный месяц.<br>
  #end
  Function GetVolumeByDate(pIn : comp; pMonth, pYear : word) : double;

end;

VipInterface VolumeMBP Implements objVolumeMBP
Licensed (FREE)
Parameters(pCommand : word; pNRec : comp);

#end // __VOLUME_MBP_VIH__
