//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.10 - Основные средства
// Настройка шаблона инвентарного номера
//------------------------------------------------------------------------------
#ifndef __IPATTERN_VIH__
#define __IPATTERN_VIH__

#Component "F_OS"

#doc
Объектный интерфейс настройки шаблона номера в ОС/НМА
#end

ObjInterface ObjPatternOS;
//------------------------ Методы интерфейса -----------------------------------
  #doc
  Инициализация
  #end
  Function Init(_wTiDk, _wType, _wMode : word; _TuneObj: LongInt; _TuneUser: comp) : word;
//------------------------------------------------------------------------------
  #doc
  Функция разбора строки шаблона и занесения данных в таблицу QPattern
  #end
  Function ParsePatternEx(_TuneCode: string; _TuneObj: longint; _TuneUser: comp) : boolean;
//------------------------------------------------------------------------------
  #doc
  Функция сборки строки шаблона номера из таблицы QPattern в формате для хранения
  #end
  Function MakePattern : string;
//------------------------------------------------------------------------------
  #doc
  Функция заполнения таблицы QPattern по умолчанию
  #end
  Function DefaultQPattern(wMaxElement : word) : boolean;
//------------------------------------------------------------------------------
  #doc
  Функция загрузки таблицы QPattern для анализа шаблона номера пользователя
  #end
  Function LoadQPatternEx(psTuneCode: string; pTuneObj: longint; pTuneUser: comp; pMode: word) : boolean;
//------------------------------------------------------------------------------
  #doc
  Именованное представления строки шаблона пользователя
  // pwMode = 0 - шаблон
  //          1 - полные наименования составляющих (без указания длин)
  // and      2 - сокращенные наменования составляющих
  // and      4 - длины состаляющих в скобках
  #end
  Function NameTunePattern(psTuneCode: string; pTuneObj: longint; pTuneUser: comp; pMode: word) : string;
//------------------------------------------------------------------------------
End;

VipInterface IPattern implements ObjPatternOS
  Parameters (TipDk : word; wType : word;  pwMode : word; TuneObj: LongInt; TuneUser : comp)
  Licensed (Free)
;
#end // __IPATTERN_VIH__
