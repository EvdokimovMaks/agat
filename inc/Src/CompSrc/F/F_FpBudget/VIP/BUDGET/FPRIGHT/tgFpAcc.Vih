//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 9.10 - модуль "Управление бюджетом"
// Базовый объект : "Режимы доступа к бюджетным сущностям"
//------------------------------------------------------------------------------

#ifndef _ObjFpAccess_Included_
#define _ObjFpAccess_Included_

#ifdef  ComponentVersion
#component "F_FpBudget"
#endif//ComponentVersion

//------------------------------------------------------------------------------
#doc
Базовый объект : "Режимы доступа к бюджетным сущностям"<br>
<b>Внимание!</b> Интерфейс предназначен только для внутреннего использования<br>
#end
#IntPrivate ObjInterface tFpAccessObjInterface;

//------------------------------------------------------------------------------
#doc
   Функция возвращает описание последней ошибки
#end
function GetStLastError : string;

//------------------------------------------------------------------------------
#doc
   Проверяет, доступна ли для редактирования бюджета,
   текущему пользователю, данная т/ф по данному ЦО
#end
function isTFEditAv(_cTForm : comp; _cCO : comp; isMessage : boolean) : boolean;

//------------------------------------------------------------------------------
#doc
   Проверяет, доступна ли для просмотра бюджета,
   текущему пользователю, данная т/ф по данному ЦО
#end
function isTFViewAv(_cTForm : comp; _cCO : comp; isMessage : boolean) : boolean;

//------------------------------------------------------------------------------
#doc
   Проверяет, доступна ли текущему пользователю, данная т/ф по данному ЦО
#end
function isTFAv(_cTForm : comp; _cCO : comp; isMessage : boolean) : boolean;

//------------------------------------------------------------------------------
#doc
   Проверяет, доступен ли для редактирования бюджет,
   текущему пользователю, по данному ЦО
#end
function isCOEditAv(_cCO : comp; isMessage : boolean) : boolean;

//------------------------------------------------------------------------------
#doc
   Проверяет, доступен ли для просмотра бюджет,
   текущему пользователю, по данному ЦО
#end
function isCOViewAv(_cCO : comp; isMessage : boolean) : boolean;

//------------------------------------------------------------------------------
#doc
   Проверяет, доступен ли для бюджет, текущему пользователю, по данному ЦО
#end
function isCOAv(_cCO : comp; isMessage : boolean) : boolean;

//------------------------------------------------------------------------------
#doc
   Проверяет, доступен ли для редактирования бюджет, текущему пользователю
#end
function isBudEditAv(_cBudget : comp; isMessage : boolean) : boolean;

//------------------------------------------------------------------------------
#doc
   Проверяет, доступен ли для просмотра бюджет, текущему пользователю
#end
function isBudViewAv(_cBudget : comp; isMessage : boolean) : boolean;

//------------------------------------------------------------------------------
#doc
   Проверяет, доступен ли бюджет, текущему пользователю
#end
function isBudAv(_cBudget : comp; isMessage : boolean) : boolean;

//------------------------------------------------------------------------------
#doc
   Проверяет, доступен ли бюджетный процесс, текущему пользователю
#end
function isBudProcAv(_cBudProc : comp; isMessage : boolean) : boolean;

//------------------------------------------------------------------------------
//------------------------------------------------------------------------------
End;

VipInterface ObjFpAccess implements tFpAccessObjInterface
#Licensed_Free
;

#endif// _ObjFpAccess_Included_
