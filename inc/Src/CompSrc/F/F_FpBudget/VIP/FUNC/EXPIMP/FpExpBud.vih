//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 9.10 - модуль "Управление бюджетом"
// Базовый объект : "Экспорт бюджетов в DBF"
//------------------------------------------------------------------------------

#ifndef _ObjFpExpBud_Included_
#define _ObjFpExpBud_Included_

#include FpTypes.inc

#ifdef  ComponentVersion
#component "F_FpBudget"
#endif//ComponentVersion

//------------------------------------------------------------------------------
#doc
Базовый объект : "Экспорт бюджетов в DBF"<br>
#end
ObjInterface ObjFpExpBud;

//------------------------------------------------------------------------------
#doc
   создает массив для структуры экспорта по умолчанию<br>
#end
Public procedure CreateDefaultStructArray;

//------------------------------------------------------------------------------
#doc
   заполнения массива структуры для экспорта полями для аналитики<br>
#end
Public function FillStructArrayForAnalitFromFpValues : boolean;

//------------------------------------------------------------------------------
#doc
   создает структуру dbf-таблицы dbfTable по массиву структуры экспорта<br>
#end
Public function CreateDBFStruct(dbfTable : TPtr; LogFileHandle : TPtr) : boolean;

//------------------------------------------------------------------------------
#doc
   экспортирует значения из FpValues в dbfTable, лог пишется в LogFileHandle, экспортируемая аналитика по Xэшу в AnalitikFileName<br>
#end
Public function FilldbfTableFromFpValues(dbfTable : TPtr; Mode : word; LogFileHandle : TPtr; AnalitikFileName : string) : boolean;

//------------------------------------------------------------------------------
#doc
   инициализация экспорта: вариант бюджета, код регистра учета, типовая форма (0 - все статьи)<br>
#end
Public procedure InitExport(tcBudVar : comp; tKodReg : word; tcTForm : comp; twMode : word);

end;

VipInterface IFpExpBud implements ObjFpExpBud
#Licensed_Free
;

#end
