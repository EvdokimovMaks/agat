//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 9.10 - модуль "Управление бюджетом"
// Базовый объект : "Каталог соответствий"
//------------------------------------------------------------------------------

#ifndef __ObjKatImp_Vih__
#define __ObjKatImp_Vih__

#ifdef  ComponentVersion
#component "F_FpBudget"
#endif//ComponentVersion

//==============================================================================
#doc
Базовый объект : "Каталог соответствий"<br>
<b>Внимание!</b> Интерфейс предназначен только для внутреннего использования<br>
#end
Private ObjInterface IObjKatImpRel;  //#region ObjInterface

  //============================================================================
  //#region ERROR
  //----------------------------------------------------------------------------
  #doc
     Функция возвращает описание последней ошибки
  #end
  function GetStLastError : string;
  //----------------------------------------------------------------------------
  #doc
     Вывод сообщения последней ошибки
  #end
  function RunShowError : boolean;
  //#endregion ERROR
  //****************************************************************************


  //============================================================================
  //#region FIELDS
  //----------------------------------------------------------------------------
  #doc
     Получить наименование соответствия
  #end
  function GetKatImpName(
      _cRec     : comp    // ссылка на соответствия
  ) : string;

  //----------------------------------------------------------------------------
  #doc
     Получить код аналитики "источник"
  #end
  function GetKauSrc(
      _cRec     : comp    // ссылка на соответствия
  ) : word;
  //----------------------------------------------------------------------------
  #doc
     Установить код аналитики "источник"
  #end
  function SetKauSrc(
      _cRec     : comp;   // ссылка на соответствия
      _wKauSrc  : word    // код аналитики "источник"
  ) : boolean;

  //----------------------------------------------------------------------------
  #doc
     Получить код аналитики "приемник"
  #end
  function GetKauDst(
      _cRec     : comp    // ссылка на соответствия
  ) : word;
  //----------------------------------------------------------------------------
  #doc
     Установить код аналитики "приемник"
  #end
  function SetKauDst(
      _cRec     : comp;   // ссылка на соответствия
      _wKauDst  : word    // код аналитики "приемник"
  ) : boolean;

  //----------------------------------------------------------------------------
  #doc
     Получить аббревиатуру соответствия
  #end
  function GetAbbrByNrec(
      _cRec     : comp    // ссылка на соответствия
  ) : string;
  //----------------------------------------------------------------------------
  #doc
     Найти соответствие аналитик Галактики по аббревиатуре
  #end
  function GetNRecByGalAbbr(
      _sAbbr : string     // аббревиатура соответствия
  ) : comp;
  //#endregion FIELDS
  //****************************************************************************


  //============================================================================
  //#region ADDFLD
  //----------------------------------------------------------------------------
  #doc
     Загрузка значений дополнительный полей
  #end
  function Read_AddFld(
      _cRec     : comp;   // ссылка на соответствия
  var _wKauSrc  : word;   // код аналитики "источник"
  var _wKauDst  : word    // код аналитики "приемник"
  ) : boolean;
  //----------------------------------------------------------------------------
  #doc
     Сохранение значений дополнительный полей
  #end
  function Save_AddFld(
      _cRec     : comp;   // ссылка на соответствия
      _wKauSrc  : word;   // код аналитики "источник"
      _wKauDst  : word    // код аналитики "приемник"
  ) : boolean;
  //----------------------------------------------------------------------------
  #doc
     Удаление значений дополнительный полей
  #end
  function Del_AddFld(
      _cRec     : comp    // ссылка на соответствия
  ) : boolean;
  //#endregion ADDFLD
  //****************************************************************************

End; //#endregion ObjInterface
//******************************************************************************


//==============================================================================
VipInterface ObjKatImpRel implements iObjKatImpRel
#Licensed_Free;
Public:
  constructor Init;
  destructor  Done;
End;
//******************************************************************************


#endif//__ObjKatImp_Vih__
