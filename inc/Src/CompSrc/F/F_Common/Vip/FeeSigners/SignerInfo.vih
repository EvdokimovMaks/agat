/*
 ╔═══════════════════════════════════════════════════════════════════════════╗
 ║                     (c) 2010 корпорация ГАЛАКТИКА                         ║
 ║ Проект        : ГАЛАКТИКА                                                 ║
 ║ Система       : Подписанты                                                ║
 ║ Версия        : 8.1                                                       ║
 ║ Назначение    : Интерфейс редактирования дополнительной информации        ║
 ╚═══════════════════════════════════════════════════════════════════════════╝
*/
#ifdef ComponentVersion
#component "F_Common"
#endif
#include FeeSigners.inc

#doc
Интрфейс редактирования дополнительной информации подписанта<br>
<b>Внимание!</b> Интерфейс предназначен только для внутреннего использования<br>
#end

ObjInterface oSignerInfo;

#doc
Инициализировать интерфейс<br>
#end
Function InitInter: boolean;

#doc
Отобразить интерфейс<br>
#end
Function RunInter (arwFlag: word): boolean;

#doc
Получить установить/параметр текущего подписанта
#end
Procedure SetGetParam (cComp: comp);

#doc
Вернуть значение доп. информации
#end
Function GetSignerInfo (cComp: comp; pFlag: word): string;

#doc
Удалить запись о подписанте
#end
Procedure DeleteManual (cComp : comp);

#doc
Установлен ли флаг параметра pParam для подписанта cPers
#end
Function GetFlagDopInfo(cPers : comp; pParam : word) : boolean;

#doc
 Очистить признак установленного параметра pParam для подписанта cPers
#end
Procedure ClearFlagDopInfo (cPers : comp; pParam : word);

#doc
// Задать флаг установленного параметра pParam для подписанта cPers
#end
Procedure SetFlagDopInfo(cPers : comp; pParam : word);

end; // ObjInterface

ObjInterface oSignerInfoEvent;

event procedure OnChanging;

end;

VipInterface iSignerInfo implements oSignerInfo, oSignerInfoEvent
#ifdef ATL51
  Licensed (FREE)
#endif
;
