
#component "F_An"
#ifdef ComponentVersionInDict
#include iViewSloj.vih
#end

#GalPublic type TViewSloj_p = record as table ViewSloj with x$position;

#doc
Работа с группами аналитик.
#end
ObjInterface OneGrKAUObj1;
  #doc
  Добавить группу аналитик.
  #end
  function InsertGrKAU (Razdel    : word;
                        Attr      : word;
                        Code      : string[20];
                        Name      : string
                       )          : comp; //Nrec созданной (в случае неудачи =0)
  #doc
  Удалить группу аналитик. При isRealDel=true, проверяется наличие Hash и если имеются записи, то не удалит.
  #end
  function DeleteGrKAU (iNREC     : comp;
                        isRealDel : boolean;//реально удалить или только
                                            //перевести в активное/исключенное состояние
                        iInform   : boolean //запрашивать подтверждение
                       )          : boolean;//возвращает: удалилась ли запись
  #doc
  <b>Только для аналитик без Hash!</b><br>
  Пересоздает элементарные спецификации по заданной визуальной спецификации. <br>
  Возвращает количество созданных новых записей (SpSloj).
  #end
  function ReInsSpSloj (cViewSloj_ : comp) : LongInt;

  #doc
  <b>Только для аналитик без Hash!</b><br>
  Обновление визуальной спецификации с обновлением элементарных спецификаций. <br>
  Если bufp_ViewSloj.Nrec=0, то создается новая запись (добавляет в конец). <br>
  TipMess - тип сообщений (cgiNoMessage, cgiMessage).
  #end
  function UpdViewSloj (var bufp_ViewSloj : TViewSloj_p; TipMess : word) : boolean;

end;

VipInterface OneGrKAU implements OneGrKAUObj1
#ifdef ATL51
  licensed (FREE) Parameters (pRezim : word; pRazdel : word; pGrKAU : comp)
#end
;
