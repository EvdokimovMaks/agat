//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 9.10 - модуль "Электронная отчетность"
// Интерфейс редактирования : "Алгоритм расчета"
//------------------------------------------------------------------------------

#ifndef __TREditAlgRec_Vih__
#define __TREditAlgRec_Vih__

#component "F_TaxReport"

//==============================================================================
#doc
Интерфейс редактирования : "Алгоритм расчета"
<b>Внимание!</b> Интерфейс предназначен только для внутреннего использования
#end
Private ObjInterface ITREditAlgRec;  //#region ObjInterface

  //----------------------------------------------------------------------------
  #doc
     Получить описание последней ошибки выполнения
  #end
  function GetStLastError : string;

  //----------------------------------------------------------------------------
  #doc
     Инициализировать параметры интерфейса
  #end
  function InitParam(
    _wIntMode : word;  // режим запуска (cgiNo, cgiPick, ...) см.GLOconst.INC
    _cGetRec  : comp;  // ссылка на запись
    _dRepBeg  : date;  // дата начала    периода отчета
    _dRepEnd  : date   // дата окончания периода отчета
  ) : boolean;

  //----------------------------------------------------------------------------
  #doc
     Задать задание: перейти к заданной позиции в коде алгоритма
  #end
  procedure SetTask_JumpToPosAlgCode(
    _AlgCodeY : LongInt;  // номер строки кода
    _AlgCodeX : LongInt   // номер позиции в строке кода
  );

End; //#endregion ObjInterface
//******************************************************************************

//==============================================================================
VipInterface TREditAlgRec implements ITREditAlgRec
#Licensed_Free
Parameters (
  wIntMode  : word;  // режим запуска (cgiNo, cgiPick, ...) см.GLOconst.INC
  cGetRec   : comp;  // ссылка на запись
  dRepBeg   : date;  // дата начала    периода отчета
  dRepEnd   : date   // дата окончания периода отчета
);
Public: //#region Public
  constructor Init;
  destructor  Done;
End; //#endregion Public
//******************************************************************************

#endif//__TREditAlgRec_Vih__
