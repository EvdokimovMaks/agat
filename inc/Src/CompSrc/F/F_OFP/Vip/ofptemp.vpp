
#include Empty.vpp // sEmpty

// временная таблица для группирования
table struct sDates
(
  dDoc  : date                 // дата для сортировки
)
with index
(
 sDates02 = dDoc
);


table struct sVals
(
  cVal      : comp,
  Name      : string,
  cOfpSaldo : comp
)
with index
(
  sVals02 = cVal
);

table struct sClearAkt
(
  cClearing: comp,
  cAktOFP  : comp
)
with index
(
  byClearing = cClearing,
  byAktOFP   = cAktOFP
);

/*
table struct sLevels
(
  LevelN   : word,  // номер уровня
  LevelType: word   // тип уровня
)
with index
(
  sLevels01 = LevelN,
  sLevels02 = LevelType
);
*/
/*
// пересчет ПК
table struct sTmpOFP
(
  MeanType : word,
  cPlatMean: comp,
  cVal     : comp,
  Summa    : double,
  dDoc     : date
  cClearing: comp;
  SumFact  : double;
  isClosed : word;
)
with index
(
  sTmpOFP02 = MeanType + cPlatMean + cVal
);
*/
