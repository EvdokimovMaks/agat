#ifdef ComponentVersion
#component "M_TRANSP"
#end

ObjInterface oFunc_avt;
 Procedure DefVarForOpl; //актуализация переменных для оплат
end;

VipInterface Func_avt implements oFunc_avt
#ifdef ATL51
  Licensed (FREE)   Parameters (p1: string[30]; p2 : word; p3 : word )
#end
;