//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.1 - Производственный контур
// Объект для подтверждения выполнения сменно-суточного задания
//------------------------------------------------------------------------------

#ifdef ComponentVersion
#component "M_MnPlan"
#end

Type stPar_FactSSZ = Record
  _cMC      : Comp;  // Ccылка на предмет производства (KatMc)
  _cOper    : Comp;  // Ссылка на операцию (Marsh_Sp)
  _cState   : Comp;  // Пользовательское состояние журнала резервирования
  _cTemplate: Comp;  // Ссылка на шаблон документов для ССЗ
  cPodr     : Comp;  // Цех
  cPos      : Comp;  // Ссылка на позицию документа
  cSmena    : Comp;  // Смена
  _dDate    : Date;  // Дата ССЗ
  wKolAn    : Word;  // Номер представления
  NPoleKol  : Word;  // Кол-во в ССЗ
  NPoleKolBrak : Word;  // Кол-во брака в ССЗ
  NPoleFactPerc: Word;  // Номер поля для фактического кол-ва в ССЗ (%)
  cPers     : Comp;  // Сотрудник
End;

#doc
 Объект для подтверждения выполнения сменно-суточного задания</brief>
#end
ObjInterface oFactOperSSZ;

Function SetFactOperSSZ(stm: stPar_FactSSZ): LongInt;
end;

VipInterface FactOperSSZ implements oFactOperSSZ #Licensed_Free;
