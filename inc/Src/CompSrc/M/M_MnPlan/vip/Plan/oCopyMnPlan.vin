
#component "M_MnPlan"

//Объектные интерфейс копирования документов
#ifndef __Def_oCopyMnPlan__
#define __Def_oCopyMnPlan__

ObjInterface oCopyMnPlan;
  Function CopyPlan  : boolean;
  Function SetOwner ( var pWorkCode : comp ) : boolean;
  Function InitInter : boolean;
  Function DoneInter : boolean;
end;

VipInterface iCopyMnPlan implements oCopyMnPlan Licensed(Free);
#endif
