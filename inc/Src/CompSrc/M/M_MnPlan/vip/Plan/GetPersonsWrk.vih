#ifdef ComponentVersion
#component "M_MnPlan"
#endif

//******************************************************************************
// ObjInterface oFldOfMnPlan_RB
#ifndef __GetPersonsWrk__
#define __GetPersonsWrk__

Table Struct tmpPersonsMNF
(
  cPerson   : comp,
  sFIO      : string,
  tabNum    : word,
  sPodr     : string,
  cWrk      : comp,
  bIsMain   : boolean,
  bIsMark   : boolean,
  tFree     : time,
  tBusy     : time,
  kol       : double,
  dtStart   : _DateTime,
  dtEnd     : _dateTime,
  DisDate   : Date,
  lNumSort  : longint,
  sNumSort  : tName,
  NVisible  : word
)
With Index
(
  tmpPersons01 = cPerson,
!  tmpPersons02 = bIsMark + NVisible,
  tmpPersons03 = tabNum,
  tmpPersons04 = sFIO,
  tmpPersons05 = NVisible + lNumSort + sNumSort,
  tmpPersons06 = bIsMark + NVisible + lNumSort + sNumSort
!  tmpPersons07 = NVisible

);

ObjInterface oGetPersonsWrk;
End; // ObjInterface oFldOfMnPlan_RB

VipInterface GetPersonsWrk implements oGetPersonsWrk #Licensed_Free
#ifdef Atl51
Parameters(
  _cPrEd:comp;            //
  _Flag:word;              //1 - все,  0 - только текеущее
  _wKolAn : word;         //Номер представления
  _TuneKol_Value : word;  //будем распределять количество: 0 - фиксированное значение, 1 - максимально возможное для каждой позиции
  _cProf:comp;            //Профессия (для сотрудников)
  _cPodr:comp;            //Подразделение (для сотрудников)
  FlagMask:word;          //Первый бит - показывать ли занятость (0 - да, 1 - нет)
                          //Вторый бит - 0 - одиночный выбор, 1 - множественный (Pick.wList = 927, Pick.cRec = WrkFndPE_Hdr.Nrec)
  dDate :date;            //Дата
  _cVarPlan:comp;
  _cSpMNplan:comp;        //спецификация плана. Для работы из 34 алгоритма.
  _cBrigad: comp;         //ссылка на бригаду
  _wFild : word;          //Поле бригады в приделах СЗ
  _dFildKol_Min : double;      //Количество к распределению
  _dFildKol_Max : double      //Количество к распределению
  )
#endif
;
#endif
