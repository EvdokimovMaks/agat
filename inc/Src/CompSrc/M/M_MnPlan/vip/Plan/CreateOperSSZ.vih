//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.1 - Производственный контур
// Объект для формирования сменно-суточного задания
//------------------------------------------------------------------------------

#ifdef ComponentVersion
#component "M_MnPlan"
#end

Table Struct tmpOper
(
  cRsvOper : Comp,
  Kol      : Double,
  cOtpEd   : Comp,
  cDateBeg : Comp
)
With index
(
  tmpOper01 = cDateBeg
);

Type stPar_CreateSSZ = Record
  _cMC      : Comp;  // Ccылка на предмет производства (KatMc)
  _cOper    : Comp;  // Ссылка на операцию (Marsh_Sp)
  _cState   : Comp;  // Пользовательское состояние журнала резервирования
  _cTemplate: Comp;  // Ссылка на шаблон документов для ССЗ
  cPodr     : Comp;  // Цех
  cParty    : Comp;  // Партия
  _dDate    : Date;  // Дата ССЗ
  cSmena    : Comp;  // Смена
  wKolAn    : Word;  // Номер представления
  NPoleKol  : Word;  // Кол-во в ССЗ
  NPolePlanPerc: Word;  // Номер поля для планового кол-ва в ССЗ (%)
  NPoleTSht    : Word;  // Время штучное
  NPoleTPZ     : Word;  // Время подготовительно-заключительное
  NPoleRascenka: Word;  // Расценка
  OP           : Word;  // Тип объекта планирования
End;

#doc
 Объект для формирования сменно-суточного задания</brief>
#end
ObjInterface oCreateOperSSZ;

Function AddOperSSZ(stm: stPar_CreateSSZ): LongInt;
end;

VipInterface CreateOperSSZ implements oCreateOperSSZ #Licensed_Free;
