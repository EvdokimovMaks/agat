#ifndef __RunSrvOw__
#define __RunSrvOw__

#ifdef ComponentVersion
#component "M_MNPLAN"
#end

VipInterface RunSrvOw  // Диспетчер событий сервера
  Licensed ( ManufPlan, SalePlan, SupplyPlan, coPlan, manufacture )
  Parameters
  (
    wGPort: word;    // порт работы с сервером (по умолчанию 8080)
    iGMode: integer  // режим работы с сервером: 0/1 - master/slave
  );

#endif // __RunSrvOw__

