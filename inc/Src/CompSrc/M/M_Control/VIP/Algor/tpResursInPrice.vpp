//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.10 - Контроллинг
// Таблица в памяти, содержащая ресурсы с нулевой ценой
//------------------------------------------------------------------------------

Table struct tpListZeroPrice
(
  wResurs  : word,
  cResurs  : comp
)
With Index
(
 tpListZeroPrice01 = wResurs + cResurs
);

Table struct tpNoFormAnalit  // содержит ручные аналитики из ШД, которые в ППБ не надо формировать
(
  wAnalit_1  : word,
  wAnalit_2  : word,
  wAnalit_3  : word,
  wFantom    : word
);

Table struct tpParamDirPF  //  параметры для поиска цен директивных ПФ
(
  wCalcObj           : word,
  cCalcObj           : comp,
  c_ResourcePrice    : comp,    // для ресурсов
  c_ResourcePriceMC  : comp,
  c_ResourcePriceUsl : comp,
                                    // для ПФ  
  cResPrice          : comp,
  cPriceListMC       : comp,
  cPriceListUsl      : comp,
                                    // не нормируемые ресурсы  
  cResPriceNoNorm    : comp,
  
  dNormAmount_PB     : double,
  dVixod             : double,

  dDateTek           : date,
  cValut             : comp
);

Table struct tpStoreAlg5  //  параметры в части хранения разузловок
(
  cCalcObj   : comp,
  wRegim     : word
);

Table struct tpIzgFromPlan  //  параметр Изготовитель из позиции плана
(
  cFict : comp
);

Table struct tpParamReport  //  параметры для формирования отчета
(
  UDR_Word3    : word,
  UDR_Word4    : word,
  UDR_Word6    : word,
  UDR_Word8    : word,
  UDR_Word9    : word,
  UDR_Word10   : word,
  UDR_Word13   : word,
  UDR_Word14   : word,
  UDR_Word15   : word,
  UDR_Word16   : word,
  UDR_Word17   : word,
  UDR_Word18   : word,
  UDR_Word19   : word,
  UDR_Double1  : double
);
