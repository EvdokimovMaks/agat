//------------------------------------------------------------------------------
// !!! Не использовать в новых разработках
//------------------------------------------------------------------------------

//------------------------------------------------------------------------------
// Каша переменных, надо разгребать, как написано ниже
//------------------------------------------------------------------------------

//------------------------------------------------------------------------------
// Если нужны - использовать GetCurTunes.vpp
  CurDesGr       : TDesGr;
  CurDescr       : TDescr;
  CurVisible     : word;
//------------------------------------------------------------------------------
// Если нужны И использовался GetTune1.vpp - использовать GetUserFilterDate.vpp
  BegDate,
  EndDate        : date;
//------------------------------------------------------------------------------
// остальное предлагается переносить в интерфейсы по месту использования
  myPlansNo      : comp;            // текущий план счетов
  myRec          : comp;
  OkUserDate,
  ModOborot      : boolean;
  dFormNa        : date;
  TecNameFilter  : string;
  IntDatFil      : word;
  AllPick        : word;            // Всего выбрано записей
//------------------------------------------------------------------------------
  CurProcProtect : byte;            // см. S_Close.vpp
