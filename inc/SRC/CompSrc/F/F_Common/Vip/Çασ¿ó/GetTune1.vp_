//------------------------------------------------------------------------------
// !!! Не использовать в новых разработках,
// см. GetCurTunes.vpp
//------------------------------------------------------------------------------
function GetUserFilterDate : boolean;
{
   GetUserFilterDate := false;
   if (boGetTune('User.UseFilter'))
   {  IntDatFil := 1;
      BegDate   := dGetTune('User.dFilter1');
      EndDate   := dGetTune('User.dFilter2');
      GetUserFilterDate := true;
   }
}
//------------------------------------------------------------------------------
