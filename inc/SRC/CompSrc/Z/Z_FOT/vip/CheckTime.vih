//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 9.1 - Заработная плата
// Расчет суммы взносов по сотруднику
//------------------------------------------------------------------------------

#component "Z_Fot"

ObjInterface ICheckTime;
  function SetMethod(AName: string): boolean;
end;

VipInterface CheckTime
  implements ICheckTime
  licensed(free);
