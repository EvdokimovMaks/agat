//------------------------------------------------------------------------------
//                                                      (c) корпорация ГАЛАКТИКА
// Галактика 9.1 - Заработная плата
// Выбор единовременных пособий
//------------------------------------------------------------------------------
#component "Z_PayRep"

objinterface oPickChildPos;
  function GetMarksCount: longint;
  procedure ClearMarks;
end;

vipinterface PickChildPos implements oPickChildPos
  licensed(free) parameters(dtBeg, dtEnd: date);
