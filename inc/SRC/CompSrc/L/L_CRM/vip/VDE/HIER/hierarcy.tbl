table struct TempHier
(
   cRec        : comp       "ссылка на экземпляр сущности",
   cNode       : comp       "ссылка на верхний узел",
   Name        : string[32] "наименование элемента"
)
with index
(
  IndexIdRec     = cRec,
  IndexByNode    = cNode + Name
);
