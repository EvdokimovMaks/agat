//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 9.1 - модуль "Логистика"
// Пометка позиций в прайс-листе
//------------------------------------------------------------------------------

#ifndef __PrMarker_vih__
#define __PrMarker_vih__

#include Marker.vih

#ifdef ComponentVersion
#component "L_PRICES"
#end

#doc
Пометка сопр. документов имеющих счета-фактуры
#end
ObjInterface IPrMarker(IMarker);

//-----------------------------------------------------------------------------
// события

  #doc
  Это событие придет перед изменением пометки текущей записи, если вернуть true, то Inherited::MarkUnMark не отработает.
  #end
  Event Function EventMarkUnmark : boolean;

end;

//-----------------------------------------------------------------------------

VipInterface PrMarker (Marker) implements IPrMarker;

Public:

  Procedure IMarker.MarkUnMark;

end;

#end // __PrMarker_vih__
