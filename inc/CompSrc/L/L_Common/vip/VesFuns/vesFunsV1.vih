//******************************************************************************
//                                                      (c) корпорация Галактика
// Галактика 7.12 - Логистика
// Функции для работы с весом и массой
//******************************************************************************

// для объявления переменной необходимо использовать #include VesFuns.var (для
// обеспечения межверсионной устойчивости), в котором описана переменная oVesFuns
// с типом текущей последней версии объекта
//
// пример использования:
//      if oVesFuns.TTN_Correct_Massa( ...

//******************************************************************************
#ifNdef __VesFunsV1_vih__
#define __VesFunsV1_vih__

//******************************************************************************

#ifdef ComponentVersion
#component "L_COMMON"
#end

#doc
Описание методов для работы с весом и массой
#end
#ifdef GAL9_1
ObjInterface ObjVesFuns;
#else
ObjInterface ObjVesFunsV1;
#end

//------------------------------------------------------------------------------
#doc
функция корректировки массы
#end
function TTN_Correct_Massa(
                    var _netto      : double;
                    var _tara       : double;
                    var _brutto     : double;
                        _quest      : boolean;         // нужно ли спрашивать : TRUE - спашивать
                        _default    : word             // ответ по умолчанию
        ): boolean;
//------------------------------------------------------------------------------
#doc
возвращает всю информацию из SpMetLoad по переданному NRec
#end
Function GetAllSpMLInfo(
                        _cSpMetLoad : comp;    //NRec
                    var _VidLoad    : word;    //"0-погрузка,1-разгрузка"
                    var _cMetLoad   : comp;    //"метод погрузки"
                    var _cTipTex    : comp;    //"тип транспортного средства"
                    var _cMC        : comp;    //"МЦ"
                    var _cGroupMC   : comp;    //"группа МЦ"
                    var _GruzPod    : double;  //"грузоподъемность"
                    var _Minut      : double;  //"норма минут на ед.веса МЦ"
                    var _Price      : double   //"сдельная расцена"
        ): boolean;
//------------------------------------------------------------------------------
#doc
инициализация переменных
#end
Procedure SpMLInit(
                        _VidLoad    : word;
                        _cMetLoad   : comp;
                        _cMC        : comp;
                        _cGroupMC   : comp;
                        _GruzPod    : double;
                        _cTipTex    : comp;
                        _Minut      : double;
                        _Price      : double
        );
//------------------------------------------------------------------------------
// procedure SpMLInit
#doc
определение нормы и расценки для метода погрузки
#end
Function GetSpMLData(
                        _VidLoad    : word;    //"0-погрузка,1-разгрузка"
                        _cMetLoad   : comp;    //"метод погрузки"
                        _cMC        : comp;    //"МЦ"
                        _cGroupMC   : comp;    //"группа МЦ"
                        _GruzPod    : double;  //"грузоподъемность"
                    var _cTipTex    : comp;    //"тип транспортного средства"
                    var _Minut      : double;  //"норма минут на ед.веса МЦ"
                    var _Price      : double   //"сдельная расцена"
        ): boolean;
//------------------------------------------------------------------------------

end;

//******************************************************************************

#ifdef GAL9_1
VipInterface VesFuns implements ObjVesFuns licensed (FREE);
#else
VipInterface VesFunsV1 implements ObjVesFunsV1 licensed (FREE);
#end

#end
//******************************************************************************