
#include dwt.vih

ObjInterface IDW_ETK_Oper;
  Procedure DefaultParams;
  Procedure LoadParams(mf_handle : longint);
  Function DoExport: boolean;
End;

VipInterface dwt_ETK_Oper implements IDWTask, IDW_ETK_Oper licensed(DEMain);
