// =============================================================================
//                                                     (c)  корпорация Галактика
// Галактика 7.11 - Производственный контур
// Описание интерфейса-справочника "Алгоритмы расчета с древовидной настройкой параметров"
// =============================================================================

#component "M_MnPlan"

//******************************************************************************
// ObjInterface oFormPl_AlgCalcTP_RB
#ifndef __Def_FormPl_AlgCalcTP_RB__
#define __Def_FormPl_AlgCalcTP_RB__
#doc
 Описание интерфейса-справочника "Алгоритмы расчета с древовидной настройкой параметров"</brief>
#end
ObjInterface oFormPl_AlgCalcTP_RB;
End; // ObjInterface oFormPl_AlgCalcTP_RB

VipInterface iFormPl_AlgCalcTP_RB implements oFormPl_AlgCalcTP_RB Licensed(Free)
Parameters
(
  pcAlgCalc: comp;  // Ссылка на запись AlgCalc (FormPl.Nrec, передается/возвращается)
  pwFlRun: word;    // Группа флагов запуска
  psTitle: string;  // Заголовок окна
  pwPickList: word; // Номер Pick-листа (возвращается)
  pwSysNum: word    // Если <> 0, то фильтр по заданному системному номеру
);

#endif

