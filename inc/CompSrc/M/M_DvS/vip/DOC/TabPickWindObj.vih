

#ifndef __TabPickWindObj_Vih__
#define __TabPickWindObj_Vih__

#ifdef ComponentVersion
#component "M_DVS"
#endif

#include PickWnd.inc

#include WndEdit_TabPickWindObj.obj


ObjInterface oTabPickWind;
#doc
Инициализация объекта<br>
#end
  Function InitInter : boolean;
#doc
Деинициализация объекта<br>
#end
  Procedure DoneInter;

//--------------------------------------------------------------------------------------------------------
#doc
Возвращает свободный индекс для TabPickWind.wList на момент вызова функции<br>
#end
  Function GetFreeList_TabWindPick : word;

#doc
Удаление временных таблиц TabNameWind, TabPickWind, TabPickWind_Ext с наложенным<br>
фильтром по значениям параметров "prNameObj", "prNameWndPick"<br>
#end
  Function DeleteAllTabNameWind(prNameObj : string; prNameWndPick : string) : Boolean;

#doc
Удаление всех временных таблиц TabNameWind, TabPickWind, TabPickWind_Ext <br>
#end
  Function Delete_TabPickWind_All(prListPick : word) : boolean;

#doc
Вставка временной таблицы TabNameWind.<br>
#end
  Function Ins_TabNameWind(prNameObj : string; prNameWndPick : string) : comp;

#doc
Вставка временной таблицы TabPickWind.<br>
#end
  Function Ins_TabPickWind(prListPick : word; prcValue : comp; prNameFld : string; prNumFld : word; prPick : word; prFlagUseField : word; prSciStatus : word; prRefTabNameWind : comp) : comp;

#doc
Выборка данных из таблицы TabPickWind, компоновка выбранной информации <br>
в структуру StructPickWnd. <br>
Идентификация TabPickWind осуществляется по параметрам<br>
 prListPick - индекс группы таблиц TabPickWind<br>
 prcValue   - уникальный идентификатор для TabPickWind, который должен быть<br>
   заранее определен перед вставкой TabPickWind<br>
#end
  Function Get_S_PickWnd_from_IndexFld(prListPick : word; prcValue : comp; var S_PickWnd : StructPickWnd) : boolean;

#doc
Выборка данных первой записи таблицы TabPickWind из группы с <br>
индетификатором prListPick в структуру StructPickWnd. <br>
#end
  Function StructPickWnd_GetFirst(prListPick : word; var S_PickWnd : StructPickWnd) : comp;

#doc
Выборка данных следующей записи таблицы TabPickWind из группы с <br>
индетификатором prListPick в структуру StructPickWnd. <br>
Здесь параметр "prRefTmpTab" - ссылка на предыдущую запись таблицы<br>
#end
  Function StructPickWnd_GetNext(prListPick : word; prRefTmpTab : comp; var S_PickWnd : StructPickWnd) : comp;

#doc
Возвращает TabPickWind.sNameFld для
   prListPick            ==     TabPickWind.wList      and
   prIndexField          ==     TabPickWind.cValue
#end
  Function GetNameField(prListPick : word; prIndexField : comp) : string;


#doc
//??????????????????? zzzzzzzzzzzzzzzzzz
#end
Procedure Load_WndEdit_TabPickWindObj(prWndEdit_TabPickWindObj : oWndEdit_TabPickWind);

#doc
Вызов окна "WndSomeListPick", с возможностью группового <br>
выбора таблиц "TabPickWind"<br>
где prListPick - идентификатор заданной группы таблиц TabPickWind<br>
 prValue   - уникальный идентификатор для заданной таблицы TabPickWind<br>
#end
  Function RunWndNst_ListSomePick(prTitleWindow : string; prListPick : word; prValue : comp) : longInt;

#doc
Вызов окна "WndOneListPick", с возможностью одиночного <br>
выбора таблиц "TabPickWind"<br>
где prListPick - идентификатор заданной группы таблиц TabPickWind<br>
 prValue   - уникальный идентификатор для заданной таблицы TabPickWind<br>
#end
  Function RunWndNst_ListOnePick(prTitleWindow : string; prListPick : word; var prValue : comp) : longInt;

#doc
Для таблиц "TabPickWind" с идентификатором группы "prListPick" выполняется <br>
операция :<br>
  TabPickWind.Pick := TabPickWind.Pick_Default;<br>
где TabPickWind.Pick - флажок выбора пользователем данной записи таблицы<br>
    TabPickWind.Pick_Default - флажок выбора устанавливаемый по умолчанию<br>
#end
  Function SetDefaultPick_TabPickWind(prListPick : word) : Boolean;
#doc
Функция устанавливает флажки выбора для таблиц "TabPickWind" с идентификатором<br>
группы "prListPick".<br>
Перечень таблиц а также значение флагов выбора для каждой таблицы читается из DSK<br>
Если данные в DSK не найдены, значения флагов выбора устанавливается равным <br>
значению "по умолчанию"<br>
#end
  Function SetListPick_Init_ReadDSK(prListPick : word) : Boolean;

#doc
Сохранение значений флагов выбора для таблиц "TabPickWind" с идентификатором<br>
группы "prListPick" в DSK.<br>
#end
  Function SaveListTabPickWind_ToDSK(prListPick : word) : boolean;

#Doc
Устанавливает флажок "Поле может быть выбрано"<br>
#end
Function SetFlag_Use_Field(prRefTabPickWind : comp) : boolean;

#Doc
Устанавливает флажок "Поле не может быть выбрано"<br>
#end
Function SetFlag_UnUse_Field(prRefTabPickWind : comp) : boolean;

end;


#endif
