//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 5.80 - модуль "Контроллинг"
// Функциональный интерфейс : "Протоколы формирования первичных документов"
//------------------------------------------------------------------------------

#ifndef __MCProtCreatePD_Vih__
#define __MCProtCreatePD_Vih__

#component "M_Control"

//------------------------------------------------------------------------------
#doc
Функциональный интерфейс : "Протоколы формирования первичных документов"
#end
ObjInterface oMCProtCreatePD;

//------------------------------------------------------------------------------
#doc
   Инициализация интерфейса протоколов
#end
Function InitParam(
  _cTypeOwn : comp  // ссылка на текущий режим формирования/корректировки ПД
) : boolean;

//------------------------------------------------------------------------------
#doc
   Вставка нового протокола
#end
Function InsertNewProt(_TypeOwn : word; _cOwner : comp; _MemoFileName : string ) : boolean;

//------------------------------------------------------------------------------
#doc
   Удаление протоколов для заданной сущности
#end
Function DeleteProts(_TypeOwn : word; _cOwner : comp) : boolean;

//------------------------------------------------------------------------------
#doc
   Запуск каталога протоколов
#end
Function RunKatProt(
  _wRunMode : word; // Виды протоколов :
                    // toControl_BuildPD - формирование
                    // toControl_CheckPD - корректировка
  _wIntMode : word  // Режим запуска по битам:
                    //   cgiNo         - интерфейс редактирования
                    //   cgiPick       - интерфейс выбора
                    //   cgiSingleEdit - одиночное редактирования
) : boolean;

End;
//------------------------------------------------------------------------------

VipInterface iMCProtCreatePD implements oMCProtCreatePD
#Licensed_Free
;

#endif // __MCProtCreatePD_Vih__


