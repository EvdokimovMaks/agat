//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.10 - модуль "Платежный календарь"
// Настройка иконок для статусов журнала обязательств
//------------------------------------------------------------------------------

ObjInterface oVipRunSemaphor;
  procedure _Lock;
  procedure _UnLock;
  function _isLock: Boolean;
End;

VipInterface VipRunSemaphor implements oVipRunSemaphor Licensed(Free);
