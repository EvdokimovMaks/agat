
#ifndef o_MBP_TuneObj
#define o_MBP_TuneObj

//-----------------------------------------------------------------------------

#ifdef ComponentVersion
#component "F_MBP"
#end

//-----------------------------------------------------------------------------

#doc
В данном интерфейсе декларируются функции для работы с настройками. Интерфейс
пришел на смену vpp-шке, которая была разработана для упрощения перехода от
чтения настроек из таблицы к использованию API.<br>
#end
ObjInterface MBP_TuneObj;

#doc
Возвращает значение настройки, заданной параметром ParamID.<br>
#end
function GetMBPTune( ParamID : Word) : Word;

#doc
Устанавливает новое значение для настройки, заданной параметром ParamID.<br>
#end
function SetMBPTune( ParamID : Word; ParamVal : Word) : Boolean;

end;

//-----------------------------------------------------------------------------

VipInterface MBP_Tune implements MBP_TuneObj
#ifdef ATL51
  Licensed (FREE)
#end
;

//-----------------------------------------------------------------------------

#end
