//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.1 - Спецоснастка
// Функции для удаления старых операций спецоснастки/спецодежды
//------------------------------------------------------------------------------

#ifndef __MBP_DelInfo__
#define __MBP_DelInfo__

#ifdef ComponentVersion
#component "F_MBP"
#end

#doc
Функции для удаления старых операций спецоснастки/спецодежды.<br>
#end
ObjInterface iMBP_DelInfo;

  #doc
  Функция расчета состояния приходной операции на указанную дату.<br>
  #end
  procedure CalcStateOnDate(tNRec : comp; tDate : date; var tKol, tPerc : double);

  #doc
  Функция сохранения остатка предметов по старой приходной операции.<br>
  #end
  procedure InsSaldo(tNRec : comp; tDate : date; tKol, tPerc : double);

  #doc
  Функция удаления старой приходной операции.<br>
  #end
  procedure DelMBPIn(tNRec : comp);

  #doc
  Функция удаления старой расходной операции.<br>
  #end
  procedure DelMBPOut(tNRec : comp);

end;

VipInterface MBP_DelInfo implements iMBP_DelInfo Licensed (FREE);

#endif // __MBP_DelInfo__
