!/* NEW
table struct sTmpTaxes
(
  nRec    : comp,
  Code    : word,
  wType   : word,
  cRec    : comp,
  cRecStr : comp, // ссылка на KatKod, по которому построена группа
  cNode   : comp,
  Sum     : array [1..2] of double // для акта сверки
)
with index
(
  byNRec = nRec (unique, surrogate),
  byNode = Code + cNode,
  byNode1 = Code + wType + cRec + cNode,
  byRec  = Code + wType + cRecStr
)
;
!*/

/*
// суммы дерева
table struct sTreeSums
(
  cRec     : comp,       // cTempDescr
  Sum      : array [1..2] of double
)
with index
(
  byGroup = cRec + Npp
);
*/

table struct sTmpFilial
(
  nRec    : comp,
  wType   : word,
  cRec    : comp,
  cNode   : comp
)
with index
(
  byNode = cNode,
  byRec = wType + cRec
)
;
