//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.1 - Бухгалтерский контур
// Константы для режимов проверки ХозОпераций документа
//------------------------------------------------------------------------------

#ifndef __ChkSoprHoz_inc__
#define __ChkSoprHoz_inc__

Const
  cgMakeSH       = 0; // при несовпадении сумм документа и ХозОпераций по нему,
                      // корректировать ХозОперации (напр. после редактирования документа)
  cgMakeDoc      = 1; // можно корректировать документ, если его сумма не совпадает с суммой ХО
                      // (возможно после редактирования конкретной ХозОперации или ее проводок)
  cgSilentMakeSH = 3; // то же, что и cgMakeSH, но без мессаг подтверждения

  cgSetDescr     = 4; // установка нового дескриптора в документе
End;

#end

