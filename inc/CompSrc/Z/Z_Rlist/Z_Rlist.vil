//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.1 - Заработная плата
// Печать расчетных листков
//------------------------------------------------------------------------------

#component "Z_RList"

VipInterface RList licensed(free);
VipInterface RListPar
  licensed
  (
    #include RListPar.lih
  )
  parameters(prOne: word);

VipInterface RListRab
  licensed(free)
  parameters(prOne: word);
