//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика ERP 8.1 - Управление персоналом
// Подсчет количества элементов справочника АТД с заданной точкой входа
//------------------------------------------------------------------------------

ObjInterface IAtdCounter;
  Function Count(_Entry: integer): longint;
End;

VipInterface AtdCounter
  implements IAtdCounter
  licensed(free);
