//------------------------------------------------------------------------------
//                                                      (c) корпорация Галактика
// Галактика 8.10 - Заработная плата
// Индексы в адресах по КЛАДР
//------------------------------------------------------------------------------

#component "Z_Zar"

#ifdef ComponentVersionInDict
  #include IAddressN.VIH
  public type TAddressN = record as table AddressN;
#end

ObjInterface IGetZIP;

#doc
поиск вхождения Building в интервал DBFBuilding по правилам структуры KLADR
#end
function FindZIP(DBFbuilding: string; Building: string): boolean;
#doc
преобразование номера дома к формату КЛАДР
#end
function GetFormattedBuilding(b: type$AddressN): string;
#doc
поиск индекса по ссылке на улицу
#end
function GetZIPfromStreetRef(coStr: comp; sBuild: string): string;
#doc
поиск индекса из буфера адреса
#end
function GetZIPfromBuffer(b: type$AddressN): string;
end;

VipInterface GetZIP
  implements IGetZIP
  licensed(free);
