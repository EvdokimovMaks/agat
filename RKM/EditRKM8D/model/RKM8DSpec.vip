#include RKM8D.vih
#include System.vih

#component "AGAT"

interface RKM8DSpec;

var
  _items: system::ObservableCollection noauto;

procedure OnItemsChanged(collection: ICollection; args: INotifyCollectionChangedEventArgs);
{

}

constructor Init;
{
  result := loadvipref(_items, 'system::ObservableCollection');
  BindEvent(OnItemsChanged, Items.CollectionChanged);
}

property Items:system::ObservableCollection read _items;
end.
