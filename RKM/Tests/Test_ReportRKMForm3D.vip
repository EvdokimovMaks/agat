#include utest.vih
#include LogService.vih
#include Query.vih

#component "TEST"

vipinterface test_ReportRKMForm3D implements oUTest, oLogServiceSet;

interface test_ReportRKMForm3D;

var _logService: oLogService;

procedure SetLogService(logService: oLogService); { _logService := logService; }

function GetDescription: string; { result := 'RKM/tests/test_ReportRKMForm3D.vip test_ReportRKMForm3D Работает только в Teach'; }

function Test: boolean;
{
  runinterfacenomodal(AGAT::ReportRKMForm3D, 281474976712044);
  result := true;
}
end.
