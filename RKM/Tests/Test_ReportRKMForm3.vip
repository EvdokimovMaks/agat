#component "TEST"

interface test_ReportRKMForm3;

create view
as select *
from mnplan
where (( 4 == mnplan.typeplan ));

screen scmain;
show(,,,3) fixed_y;
buttons
  cmDook, default;
<<
 <. Print .>
>>
end;

browse br1;
show(,4,,);
table mnplan;
fields
  mnplan.number 'Номер': [20], protect;
  mnplan.name 'Наименование': [50], protect;
end;

handleevent
  cmdook: runinterfacenomodal(AGAT::ReportRKMForm3, mnplan.nrec, '');;
end;
end.
