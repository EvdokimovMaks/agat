#include AgatKartStorage.vih

#component "l_sklad"

interface AgatKartStorage;

procedure StandOnMc(KatmcNrec: comp);
{
  if getfirst katmcmy where (( KatmcNrec == katmcmy.nrec )) = tsOk {};
  rereadrecord(#katmcmy);
}
end.
