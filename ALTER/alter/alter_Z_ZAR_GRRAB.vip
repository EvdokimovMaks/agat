#include GrrabHelper.vih

#component "Z_ZAR"

alter interface GRRAB;

var
  _grrabHelper: AGAT::GrrabHelper;

handleevent
cmAlt1: if message('Сформировать группы "'+SP_G_RAB.NAMEGR+' АУП+НВП", "'+SP_G_RAB.NAMEGR+' ПТП"?', YesNo) = cmYes
          _grrabHelper.CreateGrrabByPodgr(SP_G_RAB.NREC);
end;
end.
