create table externalqueries "Строки внешних запросов"
  with table_code=31501
  with replace
  with tableoptions noProtect, header, journal
  (
    id: longint "идентификатор запроса",
    str: string "текст строки запроса"
  )
  with index
  (
    iId = id
  )
;
sql procedure ExecuteExternalQuery(queryId: longint);
{
}

