#component "AGAT"

ObjInterface IAccessRightsHelper;
  function groupByName(sName: string): comp;
  function isUserInGroup(cGroup: comp; cUser: comp = 0): boolean;
end;

VipInterface AccessRightsHelper implements IAccessRightsHelper;