#include QueryStr.vih

#component "UTILS"

interface iQueryStrDecorator;
var
  _mainQueryStr: oQueryStr;

constructor QueryStrDecorator(mainQueryStr: oQueryStr);
{
  _mainQueryStr := mainQueryStr;
  result := true;
}

procedure Create(var l: longint);
{
}

end.
