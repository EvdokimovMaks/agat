#component "TEST"

interface test_Colors;
table struct t (clr: longint);
create view as select * from t;
browse br1
table t;
fields
  t.clr: protect, {font={backcolor=t.clr;}};
end;
handleevent
cminit:
{
  var i: longint;
  for (i := 1; i <= 1000; i++)
  {
    insert t set t.clr := i;
  }
}
end;
end.
