#include AG_DSQLPatParam.vih

#component "AGAT"

interface AG_DSQLStrPatParam;

procedure addFldRestriction(sFld: string; var sqlStr: longint);
{
  sqlAddStr(sqlStr, sFld + '=''' + value + '''');
}

end.