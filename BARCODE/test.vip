#include Cleverence.vih

#component "AGAT"

interface CleverenceTest;

var
  t: Cleverence::ITunes;

screen sc1;
buttons
  cmTunes;
<<
<. Показать настройки .>
>>
end;

handleevent
cmTunes:
{
  message('AppPath='+t.AppPath);//+''#13'DataPath='+t.DataPath+''#13'Name='+t.Name+''#13'Version='+t.Version);
}
cminit:
{
  if not loadvipref(t, 'Cleverence::Tunes') closeinterface(cmcancel);
}
end;

end.
