#include Cleverence.Warehouse.vih
#include olehelper.vih

#component "CLEVERENCE"

interface iFieldValue;

property FieldName: string
    read  _oleHelper.GetPropertyValue(_objectPointer, 'FieldName')
    write _oleHelper.SetPropertyValue(_objectPointer, 'FieldName', result);

property Value: string
    read  _oleHelper.GetPropertyValue(_objectPointer, 'Value')
    write _oleHelper.SetPropertyValue(_objectPointer, 'Value', result);
end.
